/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // Enable appDir (experimental feature)
    },
    // Add any additional Next.js config here if needed
  };
  
  module.exports = nextConfig;